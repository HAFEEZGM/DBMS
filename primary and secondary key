create table employee(
s_no int not null primary key,
name char(20),
designation char(20),
branch char(20));
create table workers(
serial_no int,
name char(20),
designation char(20),
branch char(20),
foreign key (s_no) references s_no(s_no) );
select * from employee
